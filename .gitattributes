# Treat binary assets as binary so git never applies line-ending conversion
# (which would corrupt them on checkout with core.autocrlf=true).
*.pdf binary
*.wav binary
*.mp3 binary
*.jpg binary
*.jpeg binary
*.png binary
*.ico binary
*.pptx binary
*.docx binary
*.xlsx binary
